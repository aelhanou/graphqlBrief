overwrite: true
schema: http://localhost:4000/graphql
generates:
  ./typesGenerated/graphql.ts:
    config:
      useIndexSignature: true
    plugins:
      - "typescript"
      - "typescript-resolvers"
